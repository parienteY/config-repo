global:
  message: Welcome to global Microservices Environment!
logging:
  level:
    root: INFO
management:
  endpoints: 
    web:
      exposure:
      includes: health, info, refresh
  endpoint:
    health:
      show-details: always
